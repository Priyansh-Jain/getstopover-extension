{
  "name": "getstopover-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "validate": "node validate-data.mjs",
    "build": "node validate-data.mjs && node build.mjs",
    "watch": "node build.mjs --watch",
    "package": "npm run build && node package.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.0",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3"
  }
}
